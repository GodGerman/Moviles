<?xml version="1.0" encoding="utf-8"?>
<!-- 
     ==========================================================================
     Archivo: item_event.xml
     Propósito: Diseño base para cada elemento individual de la lista de eventos.
     Funciona como la plantilla que el EventAdapter clonará.
     ==========================================================================
-->
<!-- 
     MaterialCardView: Contenedor en forma de "tarjeta" que provee sombras (elevación) 
     y bordes redondeados. Se utiliza para delimitar visualmente un elemento de otro 
     en listas largas.
-->
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="16dp"
    android:layout_marginVertical="6dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="2dp"
    app:strokeWidth="0dp">

    <!-- 
         ConstraintLayout: Excelente para diseñar componentes complejos sin tener 
         que anidar múltiples LinearLayouts, lo que mejora drásticamente el 
         rendimiento (Frame rate) al scrollear la lista velozmente.
    -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <!-- Categoría (título principal) -->
        <!-- 
             'app:layout_constraintHorizontal_chainStyle="spread_inside"' empuja el 
             título hacia la izquierda extrema y el estatus hacia la derecha extrema.
        -->
        <TextView
            android:id="@+id/tv_category"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="@string/item_category_placeholder"
            android:textColor="?attr/colorOnSurface"
            android:textSize="17sp"
            android:textStyle="bold"
            app:layout_constraintEnd_toStartOf="@id/tv_status"
            app:layout_constraintHorizontal_chainStyle="spread_inside"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- Badge de estatus (Cápsula redondeada) -->
        <TextView
            android:id="@+id/tv_status"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/bg_status_badge"
            android:paddingHorizontal="12dp"
            android:paddingVertical="4dp"
            android:text="@string/item_status_placeholder"
            android:textColor="@color/white"
            android:textSize="12sp"
            android:textStyle="bold"
            app:layout_constraintBottom_toBottomOf="@id/tv_category"
            app:layout_constraintEnd_toStartOf="@id/btn_edit_event"
            app:layout_constraintTop_toTopOf="@id/tv_category" />

        <!-- 
             Botón Editar: Permite modificar el evento.
             Se esconde ("gone") por defecto mediante Kotlin dependiendo de si
             la pantalla actual aprueba o no la edición rápida.
        -->
        <ImageButton
            android:id="@+id/btn_edit_event"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/btn_edit_event"
            android:src="@drawable/ic_baseline_edit_24"
            android:visibility="gone"
            app:layout_constraintBottom_toBottomOf="@id/tv_category"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="@id/tv_category"
            app:tint="@color/md_primary" />

        <!-- Divider sutil: Una línea horizontal estática de 1dp para separar la cabecera -->
        <View
            android:id="@+id/divider_top"
            android:layout_width="0dp"
            android:layout_height="1dp"
            android:layout_marginTop="12dp"
            android:background="@color/divider"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/tv_category" />

        <!-- Fecha y hora con icono embebido usando (drawableStartCompat) -->
        <TextView
            android:id="@+id/tv_datetime"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:drawablePadding="8dp"
            android:text="@string/item_datetime_placeholder"
            android:textColor="@color/md_primary"
            android:textSize="14sp"
            android:textStyle="bold"
            app:drawableStartCompat="@drawable/ic_calendar_small"
            app:drawableTint="@color/md_primary"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/divider_top" />

        <!-- 
             Descripción: Multilínea truncada ('ellipsize') a 2 líneas para 
             evitar que un evento con texto kilométrico desconfigure el tamaño 
             global de la tarjeta y arruine la experiencia de listado.
        -->
        <TextView
            android:id="@+id/tv_description"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:ellipsize="end"
            android:maxLines="2"
            android:text="@string/item_description_placeholder"
            android:textColor="?attr/colorOnSurface"
            android:textSize="14sp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/tv_datetime" />

        <!-- Ubicación + Contacto secundario concatenados -->
        <TextView
            android:id="@+id/tv_location_contact"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="@string/item_location_contact_placeholder"
            android:textColor="?attr/colorOnSurfaceVariant"
            android:textSize="12sp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/tv_description" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</com.google.android.material.card.MaterialCardView>
